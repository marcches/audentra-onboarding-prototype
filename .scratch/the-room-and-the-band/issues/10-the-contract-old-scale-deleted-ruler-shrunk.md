# 10 — The contract: the old scale deleted, the ruler shrunk

**What to build:** The old system stops existing, so no future screen can
reintroduce the compression that caused the complaint. This is the **contract**
half of the expand–contract, and it can only run once nothing reads the old
tokens.

Deleted rather than deprecated: the two type steps that made six steps live
inside five pixels, every half-step spacing utility, the duotone icon weight, and
the uppercase label. A deprecated token is a token someone uses again.

**The ruler shrinks from twelve invariants to nine, and gains a written
criterion.** Five go, because each polices an aesthetic this cycle deliberately
reversed — the signature confined to four files (which today fails the designer's
own request in CI), the rule that no Quest card may float, the rule that no sheet
may stretch, and the two closed Presence tables. Two arrive, because each is
verifiable and each answers a measured defect: one icon weight for meaning and
one for state, and no half-step in the spacing scale.

The criterion goes at the top of the file in prose, because this file has been
rewritten three times and every rewrite was caused by it holding taste still:
**assert a defect the client has reported more than once, never an aesthetic.**
Without that line there will be a fifth rewrite.

**Blocked by:** 03, 04, 05, 07, 08, 09 — every migrate batch.

**Status:** resolved

**Referências:**
- [Midday](https://mobbin.com/screens/72304bea-11b7-48f7-a870-a2263f475090) — the end state the deletions are aiming at: few spacing values, far apart, and grouping that comes from interval rather than from rule.
- [Apollo](https://mobbin.com/screens/09da790a-fc80-4bd0-b0cf-0bc5fcedc394) — facts set in lower case at a distinct size, which is what replaces the uppercase label without the row growing five little headings.

- [ ] The two deleted type steps no longer exist and nothing references them
- [ ] No half-step spacing utility exists anywhere in the source
- [ ] The duotone icon weight is gone
- [ ] No uppercase label survives in either surface
- [ ] The ruler asserts nine invariants
- [ ] Icon weight carries exactly two meanings and the ruler asserts it
- [ ] The spacing scale has no half-step and the ruler asserts it
- [ ] The four drift invariants, the z-index ladder, the animation rule and the single celebration layer all survive untouched
- [ ] The criterion for what may enter the ruler is written in the file
- [ ] `pnpm typecheck`, `pnpm test` and `pnpm lint` pass

## Answer

`micro` and `lead` deleted, the four elevation names folded into two, the decision wash and the sheet's hairline gone. The ruler is nine invariants with the criterion written at the top in prose. Both new invariants were checked by breaking them: each fails the build on the defect it names and passes once it is removed.

One deletion was taken early: "No Quest card floats" went in ticket 02, because it fails in CI on the commit that reverses it.
